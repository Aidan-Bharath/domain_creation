import numpy as np
import pandas as pd
import numba as nb
from multiprocessing import pool

def dataExtract()

def linearInt(data,np=8);

    pool = Pool(np)
    keys = data.keys()

if __name__ == "__main__":
